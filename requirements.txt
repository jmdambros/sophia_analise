flask>=3.0
python-docx>=1.1
pypdf>=4.0
cryptography>=42.0

# Instale só o(s) SDK(s) do(s) provedor(es) de IA que for usar:
openai>=1.40        # necessário se o usuário escolher "openai"
anthropic>=0.34      # necessário se o usuário escolher "anthropic"
google-genai>=0.3    # necessário se o usuário escolher "google"
